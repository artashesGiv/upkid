import DeviceProvider from './providers/device'
import IEProvider from './providers/browsers'
import ScrollPositionProvider from './providers/scroll-position'

export const providers = {
    DeviceProvider,
    ScrollPositionProvider,
    IEProvider,
}
